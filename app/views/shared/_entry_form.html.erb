<div class="bs-component">
	<%= form_tag(action: :entry) do %>
		<%= label :user, :category_id %><span class="label label-danger">必須</span><br>
		<%= collection_select :category_id, Category.all, :id,  :name %><br>

		<%= submit_tag "シャッフルランチにエントリーする", class: "btn btn-primary" %>        
	<% end %>
</div>