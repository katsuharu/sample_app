<div class="bs-component">
	<%= form_tag(action: :entry) do %>
		<%= label :user, :category_id, 'マッチングカテゴリー' %><span class="label label-danger">必須</span><br>
		<%= collection_select(:user, :category_id, Category.all, :id,  :name) %><br>

		<%= submit_tag "Let's Lunch", class: "btn btn-primary" %>        
	<% end %>
</div>