<div class="bs-component">
	<%= form_for @user do |f| %>
		<%= f.label :category_id, 'カテゴリー名' %><span class="label label-danger">必須</span><br>
		<%= f.collection_select(:category_id, Category.all, :id,  :name) %><br>
		<%= submit_tag "シャッフルランチにエントリーする", class: "btn btn-primary" %>        
 	<% end %>
</div>