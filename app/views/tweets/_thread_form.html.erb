<%= form_for(@tthread, remote: true, url: {action: 'thread_create'}) do |f| %>
	<%= f.text_area :content, size: "30x2", placeholder: "投稿内容" %>
	<%= f.hidden_field :tweet_id, :value => params[:id] %>
  	<%= f.submit "send", class: "" %>
<% end %>